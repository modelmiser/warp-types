[toolchain]
channel = "nightly-2026-03-19"
components = ["rust-src", "clippy", "rustfmt"]
# GPU kernels require nightly for abi_ptx and asm_experimental_arch.
# The type system itself works on stable Rust (1.75+).
# Pin nightly for reproducible artifact evaluation.
# BLOCKER: pred register class still missing for nvptx64 (ballot_sync).
# UPDATE POLICY: refresh pin at least quarterly. See .github/workflows/ci.yml.
